jwt:
  secret: MySuperSecretKeyThatIsAtLeast32BytesLongForTestingPurposes123!
  expiration: 86400000

spring:
  flyway:
    enabled: false
    locations: classpath:db/migration
  jpa:
    hibernate:
      ddl-auto: create-drop